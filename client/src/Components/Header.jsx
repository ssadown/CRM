import React from 'react';
import Navigation from '../UI/Navigation';

const Header = () => {
    return (
        <header>
            <Navigation/>
        </header>
    );
}

export default Header;
